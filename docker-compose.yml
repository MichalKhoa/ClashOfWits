services:
  bot:
    build: .
    container_name: clash_of_wits_bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - DATABASE_PATH=/app/data/clash_of_wits.db
    volumes:
      - /home/michalkhoa/services/HimmelTheHero/.env:/app/.env